
@Library("Shared") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
echo "write jenkins file in git"
                script{
                    hello()
                }
            }
        }
    }
}
